import static org.junit.Assert.assertEquals;
import org.junit.Test;

public class spockTest {

    spock s = new spock();

    @Test

    public void testfileIO(){
        assertEquals(s.rolls_per_hand, s.rolls_per_hand);
    }

    
}